{
  "name": "hook-headlines-generator-pro",
  "version": "1.0.0",
  "description": "Professional Hook & Headlines Generator with User Authentication and Freemium Model",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "start": "python -m http.server 3000 || python3 -m http.server 3000 || npx serve .",
    "deploy": "vercel --prod",
    "test": "echo 'No tests configured yet'"
  },
  "keywords": [
    "hooks",
    "headlines", 
    "copywriting",
    "marketing",
    "saas",
    "freemium",
    "gpt",
    "ai"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "tailwindcss": "^3.3.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "vercel": {
    "functions": {
      "api/generate-hooks.js": {
        "maxDuration": 30
      }
    }
  }
}